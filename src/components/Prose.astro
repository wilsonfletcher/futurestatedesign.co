---
interface Props {
  tag?: string;
  theme?: string;
}

const { tag: Tag = 'div', theme = 'yellow', ...attrs } = Astro.props as Props;
---

<Tag
  class:list={['prose', 'md:prose-xl', `prose-${theme}`]}
  {...attrs}
>
  <slot />
</Tag>
