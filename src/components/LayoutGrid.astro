---
const cols = [1, 2, 3, 4, 5, 6, 7, 8]
---

<div class="fixed inset-0 z-50 flex flex-col pointer-events-none">
  <div class="container flex flex-auto">
    <div class="grid grid-cols-8 gap-4 flex-auto md:gap-10">
      {cols.map((col) => <div class="bg-red/10" />)}
    </div>
  </div>
</div>
