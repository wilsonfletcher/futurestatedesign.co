<ol class="not-prose quux">
  <slot />
</ol>

<style is:global>
  .quux {
    @apply grid gap-4 md:grid-cols-2 md:gap-10 list-none pl-0;

    counter-reset: step;

    li {
      @apply border-t-2 border-white/30 py-8;

      &:before {
        @apply block text-[72px] md:text-[110px] leading-none text-white/50 mb-8;

        counter-increment: step;
        content: '0' counter(step);
      }

      &:last-of-type() {
        @apply bg-red;

        &:before {
          counter-increment: step;
          content: counter(step);
        }
      }
    }
  }
</style>
