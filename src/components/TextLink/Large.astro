---
import { Icon } from 'astro-icon';

export interface Props {
  name: string;
  url: string;
}

const { name, url, ...attrs } = Astro.props as Props;
---

<a class="flex items-center text-[60px]" href={url} {...attrs}>
  <span>{name}</span>
  <Icon name="arrow/45/32" />
</a>
