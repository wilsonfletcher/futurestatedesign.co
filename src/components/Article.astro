---
import { Prose } from '@/components/index.astro'

interface Props {
  class?: string
}

const { class: className = 'py-10 lg:py-20', ...attrs } = Astro.props as Props
---

<div class:list={[className]} {...attrs}>
  <div class="container">
    <div class="lg:grid lg:gap-10 lg:grid-cols-10">
      <div class="lg:col-start-4 lg:col-span-7">
        <Prose>
          <slot />
        </Prose>
      </div>
    </div>
  </div>
</div>
