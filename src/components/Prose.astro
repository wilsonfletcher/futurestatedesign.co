---
interface Props {
  class?: string
}

const { class: className = 'prose-black', ...attrs } = Astro.props as Props
---

<div class:list={['prose', 'prose-lg', 'lg:prose-xl', className]} {...attrs}>
  <slot />
</div>
