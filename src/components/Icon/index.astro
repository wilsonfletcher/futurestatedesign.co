---
interface Props {
  class?: string
  viewBox?: string
}

const {
  class: className,
  viewBox = '0 0 20 20',
  ...attrs
} = Astro.props as Props
---

<svg viewBox={viewBox} class:list={['w-[1em] h-[1em]', className]} {...attrs}
  ><slot />
</svg>
