---
import { Schema } from 'astro-seo-schema'

// import { SITE } from '@config'
import BaseHead from '@components/BaseHead.astro'
import Header from '@components/Header.astro'
import Footer from '@components/Footer.astro'
import NavPrimary from '@components/Nav/Primary.astro'

import '@styles/global.css'
import Contact from '@components/Contact.astro'
// import Notification from '@components/Notification.astro'

export interface Props {
  title?: string
  description?: string
  url?: string
  image?: {
    url?: string
    width?: number
    height?: number
    alt?: string
  }
  noindex?: boolean
}

const { title, description, url, image, noindex = false } = Astro.props as Props
const meta = { title, description, url, image, noindex }
---

<!DOCTYPE html>
<html lang="en">
  <BaseHead {...meta}>
    <slot name="head" />
  </BaseHead>
  <body>
    <div class="flex flex-col w-full min-h-screen">
      <Header />
      <main class="flex-auto">
        <slot />
      </main>
      <Contact />
      <article class="py-20 bg-[#F7F7F7]">
        <div class="container">
          <div class="lg:pl-foo">
            <p>Insights into new innovations in digital business, interesting trends and new ways of thinking about them.  </p>
          </div>
        </div>
      </article> 
      <NavPrimary />
      <Footer />
    </div>
  </body>
</html>
