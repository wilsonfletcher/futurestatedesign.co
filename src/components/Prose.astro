---
interface Props {
  class?: string;
  tag?: string;
}

const { tag: Tag = 'div', class: className, ...attrs } = Astro.props as Props;
---

<Tag
  class:list={['prose', 'prose-default', 'prose-lg', 'md:prose-xl', className]}
  {...attrs}
>
  <slot />
</Tag>
